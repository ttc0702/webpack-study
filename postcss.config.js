module.exports = {
  plugins: [
    [
      // postcss-preset-env 和 autoprefixer 可以补充样式的厂商前缀，postcss-preset-env 是 autoprefixer 的超集
      'postcss-preset-env'
      // 'autoprefixer'
    ]
  ]
};